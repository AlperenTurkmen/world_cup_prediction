# Supabase — server-only. Never import the service-role key into a Client Component.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Admin login
ADMIN_PASSWORD=
# Random string used to sign the admin cookie (e.g. `openssl rand -hex 32`)
AUTH_SECRET=
